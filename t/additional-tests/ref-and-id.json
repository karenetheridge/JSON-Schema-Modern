[
    {
        "description": "$id must be evaluated before $ref to get the proper $ref destination (with $anchors)",
        "schema": {
            "$id": "/base.json",
            "$ref": "#bigint",
            "$defs": {
                "bigint": {
                    "$comment": "a valid URI for this location: /base.json#bigint",
                    "$anchor": "bigint",
                    "maximum": 10
                },
                "smallint": {
                    "$comment": "a valid URI for this location: /#bigint",
                    "$id": "/",
                    "$anchor": "bigint",
                    "maximum": 2
                }
            }
        },
        "tests": [
            {
                "description": "data is valid against first definition",
                "data": 5,
                "valid": true
            },
            {
                "description": "data is invalid against first definition",
                "data": 50,
                "valid": false
            }
        ]
    },
    {
        "description": "$id must be evaluated before $ref to get the proper $ref destination",
        "schema": {
            "$id": "/base/base.json",
            "$ref": "int.json",
            "$defs": {
                "bigint": {
                    "$comment": "a valid URI for this location: /base/int.json",
                    "$id": "int.json",
                    "maximum": 10
                },
                "smallint": {
                    "$comment": "a valid URI for this location: /int.json",
                    "$id": "/int.json",
                    "maximum": 2
                }
            }
        },
        "tests": [
            {
                "description": "data is valid against first definition",
                "data": 5,
                "valid": true
            },
            {
                "description": "data is invalid against first definition",
                "data": 50,
                "valid": false
            }
        ]
    }
]
